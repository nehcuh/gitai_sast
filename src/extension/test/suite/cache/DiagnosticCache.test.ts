import * as assert from 'assert';
import { DiagnosticCache } from '../../../src/cache/DiagnosticCache';
import { Finding } from '../../../src/core/types';

suite('DiagnosticCache Test Suite', () => {
  test('should get and set cache', () => {
    // TODO: 实现 test
    // 暂时验证可以创建实例
    assert.ok(DiagnosticCache);
  });

  test('should expire cache', () => {
    // TODO: 实现 test
    assert.ok(DiagnosticCache);
  });

  test('should evict oldest cache item', () => {
    // TODO: 实现 test
    assert.ok(DiagnosticCache);
  });
});
