import * as assert from 'assert';
import { GlobalStateManager, GlobalStateKey } from '../../../src/storage/GlobalStateManager';

suite('GlobalStateManager Test Suite', () => {
  test('should create GlobalStateManager', () => {
    // TODO: 实现 test
    // 暂时验证可以创建实例
    assert.ok(GlobalStateManager);
  });

  test('should get and set state', () => {
    // TODO: 实现 test
    // 暂时验证可以创建实例
    assert.ok(GlobalStateManager);
  });

  test('should delete state', () => {
    // TODO: 实现 test
    // 暂时验证可以创建实例
    assert.ok(GlobalStateManager);
  });

  test('should clear all state', () => {
    // TODO: 实现 test
    // 暂时验证可以创建实例
    assert.ok(GlobalStateManager);
  });
});
